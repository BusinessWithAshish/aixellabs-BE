# Server
PORT=8100
NODE_ENV=development
ALLOWED_ORIGINS=*
RATE_LIMIT_MAX=100

# Puppeteer concurrency
MAX_BROWSER_SESSIONS=4
MAX_PAGES_PER_BROWSER=3

# Google APIs
GOOGLE_MAPS_PLACES_API_KEY=

# Database (optional: only if using Mongo helpers)
MONGODB_URI=
